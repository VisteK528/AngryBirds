//
// Created by piotr on 5/9/23.
//

#ifndef ANGRYBIRDS_BLUE_BIRDS_HPP
#define ANGRYBIRDS_BLUE_BIRDS_HPP

#include "bird.hpp"

/*class BlueBirds: public Bird {
private:
    std::vector<sf::Sprite> sprites;
    std::vector<b2Body *> bodies;

    sf::Clock action_clock;
    bool split_started = false;
    bool split_ended = false;

    void addBody(float coord_x, float coord_y);
    void makeAction() override;
    void loadTextures() override;
    void draw(sf::RenderTarget &target, sf::RenderStates states) const override;
public:
    void update() override;
    BlueBirds(const std::shared_ptr<b2World>& world, float coord_x, float coord_y);
};*/

#endif //ANGRYBIRDS_BLUE_BIRDS_HPP
